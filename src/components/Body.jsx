export default function Body() {

  return (
    <section>
      <p>Practicing the process of passing props, such as strings, integers, and objects, to a component in React.</p>
    </section>
  );
}
